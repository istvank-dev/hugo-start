{
    "name": "Hugo + Tailwind Dev Container",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash"
            },
            "extensions": [
                "golang.go"
            ]
        }
    },
    //"postCreateCommand": "node -v && npm -v && hugo version",
    "forwardPorts": [1313],
    "remoteUser": "vscode"
}
